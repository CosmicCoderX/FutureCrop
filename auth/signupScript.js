// Signup specific optimizations (if needed, can be minimal since auth.js handles it)
document.addEventListener('DOMContentLoaded', () => {
  // Add any signup-specific enhancements here
  console.log('Signup page loaded');
});
